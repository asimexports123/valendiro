import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function AuthPage() {
  return (
    <AdminPlaceholder
      title="Auth"
      description="Auth module placeholder. This page will host the management interface for users, roles, and access control."
    />
  );
}
