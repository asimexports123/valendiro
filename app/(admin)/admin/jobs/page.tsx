import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function JobsPage() {
  return (
    <AdminPlaceholder
      title="Jobs"
      description="Jobs module placeholder. This page will host the management interface for cron jobs and automation tasks."
    />
  );
}
