import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function SettingsPage() {
  return (
    <AdminPlaceholder
      title="Settings"
      description="Settings module placeholder. This page will host the management interface for system configuration and feature flags."
    />
  );
}
