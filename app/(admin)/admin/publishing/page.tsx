import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function PublishingPage() {
  return (
    <AdminPlaceholder
      title="Publishing"
      description="Publishing module placeholder. This page will host the management interface for content publishing lifecycle and schedule."
    />
  );
}
