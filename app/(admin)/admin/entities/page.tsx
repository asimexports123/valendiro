import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function EntitiesPage() {
  return (
    <AdminPlaceholder
      title="Entities"
      description="Entities module placeholder. This page will host the management interface for people, organizations, products, and concepts."
    />
  );
}
